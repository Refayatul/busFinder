package com.rex.busfinder.ui.theme

import androidx.compose.ui.graphics.Color

// Default Material 3 colors (you can keep or remove if not explicitly used)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)


// --- LIGHT THEME COLORS (Material 3 Roles) ---
val LightPrimary = Color(0xFF1976D2) // Your existing Primary
val LightOnPrimary = Color(0xFFFFFFFF) // Your existing OnPrimary
val LightPrimaryContainer = Color(0xFFBBDEFB) // Derived from your LightPrimaryVariant or a lighter shade
val LightOnPrimaryContainer = Color(0xFF0D47A1) // Darker text for light container

val LightSecondary = Color(0xFF03DAC6) // Your existing Secondary
val LightOnSecondary = Color(0xFF000000) // Your existing OnSecondary
val LightSecondaryContainer = Color(0xFF64FFDA) // A lighter shade of secondary
val LightOnSecondaryContainer = Color(0xFF004D40) // Darker text for light container

val LightTertiary = Purple40 // Using your existing Purple40
val LightOnTertiary = Color(0xFFFFFFFF)
val LightTertiaryContainer = Color(0xFFEADDFF) // Standard M3 tertiary container
val LightOnTertiaryContainer = Color(0xFF21005D) // Standard M3 on tertiary container

val LightBackground = Color(0xFFFFFFFF) // Your existing Background
val LightOnBackground = Color(0xFF000000) // Your existing OnBackground

val LightSurface = Color(0xFFFFFFFF) // Your existing Surface
val LightOnSurface = Color(0xFF000000) // Your existing OnSurface
val LightSurfaceVariant = Color(0xFFE0E0E0) // A subtle variation of surface
val LightOnSurfaceVariant = Color(0xFF424242) // Text color for surface variant

val LightError = Color(0xFFB00020) // Your existing Error
val LightOnError = Color(0xFFFFFFFF) // Your existing OnError
val LightErrorContainer = Color(0xFFFCD8DF) // Lighter error container
val LightOnErrorContainer = Color(0xFF8C0009) // Darker text for error container

val LightOutline = Color(0xFF757575) // A neutral grey for outlines
val LightScrim = Color(0xFF000000).copy(alpha = 0.60f) // Standard scrim color


// --- DARK THEME COLORS (Material 3 Roles) ---
val DarkPrimary = Color(0xFF64B5F6) // Your existing Primary
val DarkOnPrimary = Color(0xFF000000) // Your existing OnPrimary
val DarkPrimaryContainer = Color(0xFF0D47A1) // Derived, darker container
val DarkOnPrimaryContainer = Color(0xFFBBDEFB) // Lighter text for dark container

val DarkSecondary = Color(0xFF03DAC6) // Your existing Secondary
val DarkOnSecondary = Color(0xFF000000) // Your existing OnSecondary
val DarkSecondaryContainer = Color(0xFF004D40) // Darker secondary container
val DarkOnSecondaryContainer = Color(0xFF64FFDA) // Lighter text for dark container

val DarkTertiary = PurpleGrey80 // Using your existing PurpleGrey80
val DarkOnTertiary = Color(0xFF381E72)
val DarkTertiaryContainer = Color(0xFF4F378B)
val DarkOnTertiaryContainer = Color(0xFFEADDFF)

val DarkBackground = Color(0xFF121212) // Your existing Background
val DarkOnBackground = Color(0xFFFFFFFF) // Your existing OnBackground

val DarkSurface = Color(0xFF1E1E1E) // Your existing Surface
val DarkOnSurface = Color(0xFFFFFFFF) // Your existing OnSurface
val DarkSurfaceVariant = Color(0xFF424242) // A subtle variation of surface
val DarkOnSurfaceVariant = Color(0xFFBBBBBB) // Text color for surface variant

val DarkError = Color(0xFFCF6679) // Your existing Error
val DarkOnError = Color(0xFF000000) // Your existing OnError
val DarkErrorContainer = Color(0xFF8C0009) // Darker error container
val DarkOnErrorContainer = Color(0xFFFCD8DF) // Lighter text for error container

val DarkOutline = Color(0xFF9E9E9E) // A neutral grey for outlines
val DarkScrim = Color(0xFF000000).copy(alpha = 0.60f) // Standard scrim color


// --- BLUE THEME COLORS (Material 3 Roles) ---
val BluePrimary = Color(0xFF2196F3) // Your existing Primary
val BlueOnPrimary = Color(0xFFFFFFFF) // Your existing OnPrimary
val BluePrimaryContainer = Color(0xFFBBDEFB) // Derived from your BluePrimaryVariant or lighter shade
val BlueOnPrimaryContainer = Color(0xFF0D47A1) // Darker text for light container

val BlueSecondary = Color(0xFF03A9F4) // Your existing Secondary
val BlueOnSecondary = Color(0xFFFFFFFF) // Your existing OnSecondary
val BlueSecondaryContainer = Color(0xFF81D4FA) // Lighter secondary container
val BlueOnSecondaryContainer = Color(0xFF01579B) // Darker text for light container

val BlueTertiary = Purple40 // Using your existing Purple40 (or pick a blue-ish tertiary)
val BlueOnTertiary = Color(0xFFFFFFFF)
val BlueTertiaryContainer = Color(0xFFEADDFF) // Default for now
val BlueOnTertiaryContainer = Color(0xFF21005D) // Default for now

val BlueBackground = Color(0xFFE3F2FD) // Your existing Background
val BlueOnBackground = Color(0xFF000000) // Your existing OnBackground

val BlueSurface = Color(0xFFFFFFFF) // Your existing Surface
val BlueOnSurface = Color(0xFF000000) // Your existing OnSurface
val BlueSurfaceVariant = Color(0xFFD4E3F7) // A subtle variation of surface
val BlueOnSurfaceVariant = Color(0xFF424242) // Text color for surface variant

val BlueError = Color(0xFFF44336) // Your existing Error
val BlueOnError = Color(0xFFFFFFFF) // Your existing OnError
val BlueErrorContainer = Color(0xFFFCD8DF) // Lighter error container
val BlueOnErrorContainer = Color(0xFF8C0009) // Darker text for error container

val BlueOutline = Color(0xFF757575) // A neutral grey for outlines
val BlueScrim = Color(0xFF000000).copy(alpha = 0.60f) // Standard scrim color